import { OpenAreaDirective } from './open-area.directive';

describe('OpenAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenAreaDirective();
    expect(directive).toBeTruthy();
  });
});
